# Easy

## Keyword Transposition Cipher

```python
n = int(input())

for i in range(n):
    key = input()
    cipher = input()

    print(sorted(set(cipher)))
    
print(keys)
print(ciphers)
```

# Medium

## PRNG Sequence Guessing

```python
```

# Hard

## Basic Cryptanalysis

```python
```